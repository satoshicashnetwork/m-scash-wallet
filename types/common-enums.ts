export const KEYS = {
    CURRENT_ADDRESS_KEY: 'CURRENT_ADDRESS_KEY',  // 当前钱包地址标识
}


export const FILE_NAME = {
    WALLET_INFO: 'wallet_encrypted.txt',  // 存放钱包全量数据（高级加密）
    WALLET_LIST: 'wallet_list.txt', // 当前APP存放的钱包集合（明文数据）
}